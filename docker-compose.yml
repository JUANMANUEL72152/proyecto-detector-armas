services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: detector-armas-backend
    ports:
      - "5000:5000"
    environment:
      CAMERA_INDEX: ${CAMERA_INDEX:-0}
    restart: unless-stopped
