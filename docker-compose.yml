services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: detector-armas-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: unless-stopped
